<div>
<h1>My tasks</h1>
<ul>
<% @tasks.each do |task|%>
<li><%= link_to task.name, task_path(task) %> <%= link_to "Delete", task_path(task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%></li>
<% end %>
</ul>
<hr>
<%= link_to "New task", new_task_path %>
</div>
